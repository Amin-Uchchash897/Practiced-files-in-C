/*
    String reverse without strrev() function in C.
*/

#include <stdio.h>
#include <conio.h>
#include <string.h>

void main()
{
    char titleName[30] = "Geeksforgeeks";
    char backupTitleName[30];
    int indexForTitle = 0, indexForBackup = 0, length = 0;

    // Finding length from titlename.
    while (titleName[indexForTitle] != '\0')
    {
        indexForTitle++;
        length++;
    }

    // Final operation for reversing the main string
    for (indexForTitle = 0; indexForTitle++)
    {
        
    }

    getch();
}
